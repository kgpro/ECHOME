[supervisord]
nodaemon=true

[program:gunicorn]
command=gunicorn ECHOME.wsgi:application --bind 0.0.0.0:8000 --workers 3
directory=/app
autorestart=true

[program:celery_worker]
command=celery -A ECHOME worker -l info
directory=/app
autorestart=true

[program:celery_beat]
command=celery -A ECHOME beat -l info
directory=/app
autorestart=true

